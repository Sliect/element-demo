const Components     = require('../../components.json')
const fs             = require('fs')
const chalk          = require('chalk')
const render         = require('json-templater/string')
const uppercamelcase = require('uppercamelcase')
const path           = require('path')
const endOfLine      = require('os').EOL

const OUTPUT_PATH = path.resolve(__dirname, '../../src/index.js')
const INSTALL_COMPONENT_TEMPLATE = '  {{name}}'
const IMPORT_TEMPLATE = 'import {{name}} from \'./components/{{package}}\''
const MAIN_TEMPLATE = `/* Automatically generated by './build/bin/build-entry.js' */

{{include}}

const components = [
{{install}},
]

const install = function(Vue, opts = {}) {
  components.forEach(component => {
    Vue.component(component.name, component)
  })

  Vue.prototype.$ELEMENT = {
    size: opts.size || '',
    zIndex: opts.zIndex || 2000
  }
}

if (typeof window !== 'undefined' && window.Vue) {
  install(window.Vue);
}

export default {
  version: '{{version}}',
  install,
{{list}}
}
`

const ComponentNames = Object.keys(Components)

let includeComponentTemplate = []
let installTemplate          = []
let listTemplate             = []

ComponentNames.forEach(name => {
  let componentName = uppercamelcase(name)

  includeComponentTemplate.push(render(IMPORT_TEMPLATE, {
    name: componentName,
    package: name
  }))

  installTemplate.push(render(INSTALL_COMPONENT_TEMPLATE, {
    name: componentName
  }))

  listTemplate.push(`  ${componentName}`)
})

var template = render(MAIN_TEMPLATE, {
  include: includeComponentTemplate.join(endOfLine),
  install: installTemplate.join(','+endOfLine),
  version: require('../../package.json').version,
  list: listTemplate.join(','+endOfLine)
})

fs.writeFileSync(OUTPUT_PATH, template)
console.log(chalk.green('[build entry] DONE:'), chalk.yellow(OUTPUT_PATH))